name: Azure Static Web Apps CI/CD (disabled duplicate)

on:
  workflow_dispatch:

jobs:
  disabled_duplicate_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Duplicate workflow disabled
        run: echo "This workflow is intentionally disabled. Use .github/workflows/azure-static-web-apps-deploy.yml"
